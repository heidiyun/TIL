scanf의 반환타입은 int , 4개의 서식을 다 읽었으면 4를 반환한다.

< 표준입력으로 redirection
> 표준출력으로 redirection

구조체일경우, call by value로 보내면 크기가 무한정 커질 수 있다.....
구조체 포인터를 써서 보내는 크기를 줄인다.
근데 다시 함수안에서 값을 복사해버리면 똑같은 짓을 하는 것이다.

변하는 것과 변하지 않는 것을 분리해라.

함수는 함수에 대한 주소만 가지고 있다.(이름)

rand의 시퀀스가 동일한 것은 seed가 같아서 이다. 
 -> srand(43)과 같이.
seed를 변화시킬려면 srand(time(null)) -> 시간은 계속해서 변화기 때문에
					초 단위이다. 그래서 1초에 2번 rand를 돌리면 같은 결과 값이 나온다.
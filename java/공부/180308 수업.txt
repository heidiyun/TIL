<c언어 사용자 정의 타입>
struct는 모든 변수를 필요로 할 때,
union은 여러개의 타입 중 하나의 타입의 데이터만 필요한 경우 사용.
	-> 마지막에 대입된 데이터만 유효하다.
	바이트 단위의 데이터를 분해할 때 사용할 수 있다.
		-> n = 0x12345678
		-> 12 34 56 78 이렇게
(variant,  여러가지 중에서 하나만... 상황에 따라 달라지는)-> c의 union같은 것.

enum은 상수를 관리하기 위해 사용한다.
enum은 정확한 크기를 알 수 없다. (데이터 전송 및 교환시 문제가 생긴다.)


<java>

enum상수는 상수 object
상태에 따라서 다른것을 사용해야 할 때.


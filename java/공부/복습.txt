1) 래퍼런스 복사
	장점 : 주소만 복사하면 되어서 속도가 빠르다.
	단점 : 객체의 파괴 시점을 결정 할 수 없다. (댕글링 포인터의 문제가 생긴다.)
	-> 해결하기 위해. 참조계수(reference counting)
			: 래퍼런스를 통해서 객체를 관리할 때, 객체의 파괴시점을 결정하기 위해서 쓴다.
2) 값 복사
	장점 : 객체의 파괴 시점을 알 수 있다.
	단점 : 데이터의 크기가 클수록 속도가 느려지며, 성능에 영향을 미친다.

ARC 
제대로 된 코드를 작성하지 않으면 정상적으로 객체의 메모리가 수거되지 않는다.